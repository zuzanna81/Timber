<%= form_for @album do |f| %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

<%= f.submit 'Create album', class: 'btn btn-primary' %>
<% end %>

<!-- <%= form_tag do %>
  <%= label_tag :title %>
  <%= text_field_tag :title %>
  <%= submit_tag 'Create album', class: 'btn btn-primary' %>
<% end %> -->
